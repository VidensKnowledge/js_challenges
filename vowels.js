// First I need to get a string from input or from the user

let defString = "I have a lovely bunch of coconuts";

// I need a list of vowels

let vowelArr = ["a", "e", "i", "o", "u"];


vowelArr.each do |i| {
  get i.indexOf([]);
}

console.log(defString.indexOf(vowelArr));

// Now i need to parse the defString to find the vowels;

// defString.indexOf(vowelArr);